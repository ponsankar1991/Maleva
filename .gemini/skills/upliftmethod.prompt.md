---
name: upliftmethod
description: Uplift selected controller endpoint to Java Spring Boot microservice by extracting the logic for backend document preparation and prompts while maintaining the same business logic.
---
As a Java Spring Boot developer, I want to uplift a selected controller endpoint from .net project to a Java Spring Boot microservice. 
This involves extracting the logic from old legacy .net project and create new endpoint with the same business logic.
use the following steps to achieve this:
1. Identify the controller endpoint in the .net project in file ${input:filepath:Please give file path for .net project?}  and method name  ${input:method:methodname?}that needs to be uplifted.
2. Analyze the existing legacy code deeper upto the service layer implementation  to understand the business logic and functionality of the endpoint.
3. Create a new endpoint in the selected controller in this Spring Boot project that will serve as the endpoint for the microservice.
4. Use the extracted logic from the .net controller to implement the same functionality in the new Spring Boot controller, ensuring that the business logic is preserved.
5. Try to reuse the DTO & model classes if possible to maintain consistency and reduce redundancy in the codebase.

[//]: # (3. Create a new Java Spring Boot microservice project if not already created.)

[//]: # (4. Set up the necessary dependencies and configurations for the Spring Boot project.)

[//]: # (5. Create a new controller in the Spring Boot project that will serve as the endpoint for the microservice.)

[//]: # (6. Extract the logic for backend document preparation and prompts from the .net controller and implement it in the new Spring Boot controller.)

[//]: # (7. Ensure that the new endpoint in the Spring Boot microservice maintains the same business logic and functionality as the original .net endpoint.)

[//]: # (8. Test the new Spring Boot endpoint to ensure it works correctly and meets the requirements.)

[//]: # (9. Deploy the new Spring Boot microservice and monitor its performance to ensure it operates as expected.)

[//]: # (Review the following code from #selection and #file:[index.js].)

[//]: # (Focus on ${input:goal:What is your refactoring goal?} while adhering to the coding standards in #workspace.)

[//]: # ()
[//]: # (Please provide:)

[//]: # (1. Refactored code snippet.)

[//]: # (2. List of performance improvements.)

[//]: # (   As A java spring boot developer, I want to uplift selected controller endpint to java spring boot microservice . extract the logic for backend prepare document  and prompts which can create the whole code with same busines logic)
